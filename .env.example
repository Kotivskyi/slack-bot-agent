# slack_analytics_app Environment Variables

# === Project ===
PROJECT_NAME=slack_analytics_app
DEBUG=true
ENVIRONMENT=local

# === Logfire ===
# Get your token at https://logfire.pydantic.dev
LOGFIRE_TOKEN=
LOGFIRE_SERVICE_NAME=slack_analytics_app
LOGFIRE_ENVIRONMENT=development

# === PostgreSQL ===
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=slack_analytics_app

# === AI Agent (langgraph, openai) ===
OPENAI_API_KEY=
AI_MODEL=gpt-4.1
# Model for evals LLM judge (optional, defaults to AI_MODEL if not set)
# EVAL_MODEL=gpt-4.1

# Slack API Tokens (get from api.slack.com/apps -> Your App)
# Bot token: OAuth & Permissions -> Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token
# Signing secret: Basic Information -> App Credentials -> Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret